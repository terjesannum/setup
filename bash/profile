test "$0" = "/bin/sh" -o "$0" = "sh" && test -x /usr/local/bin/bash && exec /usr/local/bin/bash -l
test "$0" = "/bin/sh" -o "$0" = "sh" && test -x /bin/bash && exec /bin/bash -l
if test -n "$BASH" -a -r $HOME/.bashrc; then
   . $HOME/.bashrc
fi
